# Calculadora de Precios de Frutas
# Este programa permite a los usuarios calcular el precio total de una fruta deseada basado en su precio 
# por kilogramo.
# El usuario ingresa el nombre de la fruta y la cantidad en kilogramos, y el programa muestra el precio total.
import os
os.system('cls' if os.name == 'nt' else 'clear')

frutas = {'Plátano':1.35, 'Manzana':0.8, 'Pera':0.85, 'Naranja':0.7}
fruta = input('¿Qué fruta quieres? ').title()
kg = float(input('¿Cuántos kilos? '))
if fruta in frutas:
    print(kg, 'kilos de', fruta, 'valen', frutas[fruta]*kg, '€')
else: 
    print("Lo siento, la fruta", fruta, "no está disponible.")